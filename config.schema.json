{
    "pluginAlias": "NaimAudioPlatform",
    "pluginType": "platform",
    "singular": true,
    "headerDisplay": "Just save the config as it is by clicking the Save button in the bottom right corner",
    "footerDisplay": "https://github.com/sicamois/homebridge-naim-audio",
    "schema": {
        "type": "object",
        "properties": {
            "name": {
                "title": "Name",
                "type": "string",
                "required": true,
                "default": "Naim Audio"
            }
        }
    }
}
